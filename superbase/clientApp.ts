const auth = () => null;
const signOut = () => null;
const signInWithEmailAndPassword = () => null;
const sendPasswordResetEmail = (email: string) => email;

export { auth, sendPasswordResetEmail, signInWithEmailAndPassword, signOut };
